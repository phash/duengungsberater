# ── Düngungsberater Produktion (VPS mit Caddy) ──
SITE_URL=https://duenger.mr-development.de

# ── PostgreSQL ──
POSTGRES_PASSWORD=REPLACE_ME

# ── JWT ──
JWT_SECRET=REPLACE_ME

# ── Supabase API Keys ──
ANON_KEY=REPLACE_ME
SERVICE_ROLE_KEY=REPLACE_ME

# ── SMTP (Docker-Mailserver) ──
SMTP_USER=noreply@mr-development.de
SMTP_PASS=REPLACE_ME

# ── E-Mail-Bestätigung (false = echte E-Mail nötig) ──
MAILER_AUTOCONFIRM=false
